<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/main_background">

    <!-- Container principal para os fragments -->
    <FrameLayout
        android:id="@+id/flFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="80dp"
        android:clipToPadding="false"
        android:paddingBottom="16dp"/>

    <!-- Nafis Bottom Navigation Bar -->
    <com.nafis.bottomnavigation.NafisBottomNavigation
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_margin="16dp"
        android:elevation="8dp"
        app:mbn_circleColor="#ffffff"
        app:mbn_backgroundBottomColor="#ffffff"
        app:mbn_countBackgroundColor="#FF6200EE"
        app:mbn_countTextColor="#ffffff"
        app:mbn_defaultIconColor="#9E9E9E"
        app:mbn_selectedIconColor="#6200EE"
        app:mbn_rippleColor="#1A6200EE"
        app:mbn_shadowColor="#1A000000"
        app:mbn_hasAnimation="true" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>